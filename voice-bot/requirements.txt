fastapi
uvicorn
python-dotenv
loguru
httpx
pyyaml
requests
# FIXED: added sarvam and groq extras — both are used by the configured
# provider stacks (Stack A: Deepgram+Groq+Sarvam TTS, Stack B: Sarvam
# STT+Groq+Murf) and weren't previously installed as dependencies at all.
#
# PINNED <2.0.0: verified directly against the installed package that
# PipelineTask and PipelineRunner — which bot.py's entire execution model
# is built on (every call constructs one of each) — are deprecated since
# 1.3.0 and scheduled for removal in 2.0.0, in favor of PipelineWorker/
# WorkerRunner. This isn't broken today (1.8.1 still fully supports them,
# just with a deprecation warning), so no migration was done here — that
# would be exactly the kind of speculative architectural change not asked
# for. But an unpinned install would silently pull 2.0.0 the day it ships
# and break the app outright. Plan a PipelineWorker/WorkerRunner migration
# before ever lifting this ceiling.
pipecat-ai[deepgram,openai,elevenlabs,silero,sarvam,groq]==1.8.1
redis

# NOTE: torch, torchvision, torchaudio, transformers, and chatterbox-tts
# were removed here. They were only needed by the local Chatterbox TTS
# service, which no longer exists in this codebase (services/chatterbox.py
# was deleted, and there's no chatterbox entry left in config.yaml's
# provider registry). Together they added several GB to every install/
# Docker image for a feature you're not using — dead weight, not "keeping
# options open." If you genuinely want local TTS back later, re-add the
# specific package your chosen local model needs at that point.
#
# ALSO NOTE: pipecat-ai is still unpinned. Run `pip freeze` once your stack
# is verified working end-to-end and commit the exact versions — an
# unpinned dependency here means a future Pipecat release could silently
# change behavior or break your bot on your next install. I didn't guess a
# pin myself since I can't verify compatibility without running the stack
# against your actual providers.
